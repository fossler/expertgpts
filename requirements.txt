# ExpertGPTs Requirements

# Core Framework
streamlit~=1.59.0

# DeepSeek API (uses OpenAI-compatible interface)
openai>=2.45.0

# YAML Configuration
pyyaml>=6.0.3

# TOML Configuration (for app_defaults.toml)
# Python 3.11+ has tomllib built-in, tomli/tomli_w are fallbacks for older versions
tomli>=2.4.1
tomli_w>=1.2.0

# Environment Variables
python-dotenv>=1.2.2

# Token counting for context length
tiktoken>=0.13.0
